{
  "name": "@auto-renewal/keeper",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain keeper worker that scans and executes due blob renewals",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@mysten/sui": "^1.0.0",
    "@mysten/walrus": "^0.1.0",
    "pino": "^8.0.0",
    "node-cron": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "vitest": "^1.0.0",
    "eslint": "^8.0.0",
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.0"
  }
}
